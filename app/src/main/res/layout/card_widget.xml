<?xml version="1.0" encoding="utf-8"?>
<!--
  七种桌面卡片组件共用的骨架。

  为什么所有卡片共用一份布局：RemoteViews 不能动态建视图，能用的只有
  「预先声明好、运行时改文字/可见性/颜色」和「往 LinearLayout 里 addView 现成的
  子布局」两条路。卡片之间的差别全在**填什么内容**上，所以这里只留骨架，
  具体内容由 CardWidgetRenderer 按 cardId 填 —— 加一种卡片不必再加一份布局。

  card_rows 留 0dp + weight=1 吃掉剩余高度：内容少时不会被拉伸（行本身 wrap_content），
  内容多时先占满中段、再谈截断。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/card_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/card_widget_light"
    android:orientation="vertical"
    android:padding="12dp">

    <LinearLayout
        android:id="@+id/card_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/card_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:textColor="#1D2433"
            android:textSize="12sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/card_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:textColor="#345DC4"
            android:textSize="11sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/card_headline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:lineSpacingExtra="2dp"
        android:maxLines="2"
        android:textColor="#1D2433"
        android:textSize="19sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/card_subline"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="#536078"
        android:textSize="12sp" />

    <LinearLayout
        android:id="@+id/card_rows"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="6dp"
        android:layout_weight="1"
        android:orientation="vertical" />

    <TextView
        android:id="@+id/card_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:includeFontPadding="false"
        android:maxLines="2"
        android:textColor="#1D2433"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/card_action"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="#345DC4"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/card_footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="#536078"
        android:textSize="11sp" />
</LinearLayout>
