# 校园助理 用户手册

> 版本：适用 v1.2.6 及相近版本 · 系统要求 Android 7.0 及以上（建议 Android 12+，液态玻璃效果最完整）

校园助理是一款面向高校学生的第三方教务客户端，接入了学校的教务系统，把课表、成绩、第二课堂、学工系统的请假与去向登记、等级考试报名、教务消息等高频操作集中到一个应用里。整个界面采用"液态玻璃"材质：折射、色散、高光都是实时计算的，按下去会形变、松手弹回来。

本文按功能模块完整介绍应用的每一项能力。

---

## 目录

1. [安装与首次启动](#1-安装与首次启动)
2. [登录与账号](#2-登录与账号)
3. [主界面总览](#3-主界面总览)
4. [课表](#4-课表)
5. [成绩与考试](#5-成绩与考试)
6. [第二课堂](#6-第二课堂)
7. [学工系统](#7-学工系统)
8. [考级项目报名](#8-考级项目报名)
9. [消息中心](#9-消息中心)
10. [「我的」页逐项说明](#10-我的页逐项说明)
11. [课程提醒](#11-课程提醒)
12. [通用操作与使用技巧](#12-通用操作与使用技巧)
13. [数据与隐私安全](#13-数据与隐私安全)
14. [常见问题 FAQ](#14-常见问题-faq)
15. [注意事项与免责声明](#15-注意事项与免责声明)

---

## 1. 安装与首次启动

### 1.1 安装

从发布页下载 APK 安装包，直接安装即可。安装时如系统提示"未知来源应用"，允许该来源安装。

> **包名说明（1.2.2 起）**：本应用的安装包标识由 `com.tyust.course` 更换为 `com.hnnujw.course`。安卓把包名当作应用身份，两个包名互相独立、**无法覆盖升级**，因此从旧版升级需要手动安装一次新版；装好后是一个全新应用，重新登录一次教务账号、同步一次课表即可恢复。旧版可以等新版确认可用后再卸载，两者也能暂时并存。
>
> 若在浏览器里下载到的文件名是 `.zip`，那是浏览器按服务器返回的类型给配的后缀（文件本身没坏），把后缀改回 `.apk` 即可安装；也可以直接使用应用内的「检查更新 → 立即更新」，由应用自己下载，不经过浏览器。

### 1.2 首次启动流程

冷启动后，应用会按以下顺序依次引导：

1. **登录页**：首次使用必须先登录教务账号（见第 2 章）。
2. **功能引导页（Onboarding）**：登录成功后的首次启动会播放 4 页横滑引导，分别介绍课表与成绩、学工系统、第二课堂、账号与设置。可以左右滑动阅读，也可以点「跳过」直接进入。

应用**永久免费、无需激活**：开源版本不含设备白名单或授权校验，装好登录就能用。

---

## 2. 登录与账号

### 2.1 三种登录方式

登录页顶部有三个分段标签：**密码登录 / Cookie 登录 / 手机号登录**（学校支持手机号通道时才显示），可随时切换。

**密码登录**

- 输入学号和密码，密码框右侧的眼睛图标可以切换明文/密文显示。
- 当学校要求图形验证码时，会弹出验证码弹窗：显示验证码图片、输入框，以及「看不清？点击刷新」按钮。
- 常见错误都有明确提示，例如「用户名或密码不正确」「验证码错误」等。

**手机号登录**

- 输入手机号 → 点「获取验证码」→ 收到短信后填入验证码 → 点「登录」。
- 走的是学校统一身份认证的手机号通道，**全程不用碰 Cookie，也不用跳出去开网页**；登录成功后自动合并统一身份认证域与教务 SSO 域名的 Cookie，直接进入主界面。
- 手机号需要事先在学校统一身份认证里绑定。

**Cookie 登录**

- 在浏览器里登录学校教务后，复制会话 Cookie，粘贴进多行输入框（默认打码显示，可切换明文），提交即可登录。
- 适合不方便输入密码、或学校需要跳转统一身份认证的场景。

**网页登录（内嵌浏览器）**

- 从登录页进入内嵌浏览器，直接打开学校教务登录页，可完整完成统一身份认证（SSO）等网页流程。
- 浏览器带地址栏（可输入网址或关键词搜索）、进度条、刷新按钮和「教务入口」一键返回。
- 登录成功后点「完成登录」，应用自动回传 Cookie 并登录。
- 出于安全考虑，非学校域名的跳转会被拦截并提示；导出的 Cookie 仅限定为学校教务地址。

### 2.2 入口选择与连通性测试

- 应用支持「主站」与「备用入口（IP 直连）」两个登录入口，登录页顶部可直接切换，切换不占设备绑定名额。
- 「测试站点连通性」按钮会对主站和备用入口各探测一次，逐行显示连通/不通、延迟（毫秒）或失败原因（域名解析失败 / 连接超时 / 证书校验未通过 / 无法建立连接）。用它来区分「账号密码错了」还是「学校网络不通」。

### 2.3 账号绑定与多账号

- 首次登录一个新账号时会弹出「确认绑定账号」对话框，显示姓名与设备绑定名额，取消则不绑定。
- 每台设备最多绑定 **3 个学生账号**（所有版本合计），超限会提示错误。
- 已保存密码的账号，会话过期时应用会尝试**自动续期**；学校要求验证码或二次确认时仍需手动完成。
- 已登录状态下再次冷启动，应用直接进入主页并提示「欢迎回来，XXX」。

### 2.4 账号管理

「我的」→ 账号与教务 → 「账号管理」把**重新登录与账号切换合并在一起**，弹窗里固定显示 3 个槽位：

- 已绑定的账号各占一框，显示姓名、学号、登录方式（密码 / Cookie）、学校、是否已存密码。
- **点击账号框即可切换**到那个账号（当前账号框有高亮标识）；框内也保留了「删除密码」「删除账号」小按钮。
- **空槽位显示加号**，点击会跳转登录页：登录一个新账号后自动绑定到本机（每台设备最多 **3 个学生账号**，超限会提示错误）。
- 各账号的课表缓存、自定义课程、提醒、消息未读数彼此独立。
- **删除密码**：只清除保存的密码，保留账号，同时取消自动续期。
- **删除账号**：连同密码、Cookie 和本地课表缓存一起删除，不可恢复；删除当前登录账号会返回登录页。删除账号不会释放设备绑定名额。

### 2.5 会话失效与重新登录

- Cookie 失效时页面会出现悬浮提示「需要重新登录 / 正在恢复登录状态」，可一键拉起重新登录页，登录后返回原页面；会话过期弹窗提供「稍后 / 立即登录」。
- 重新登录已并入「账号管理」：独立入口取消了，当前账号的会话在账号管理里点自己的账号卡即可恢复。
- 「我的」页的「更新 Cookie」可以手动刷新登录状态（适用于密码登录的账号）。

---

## 3. 主界面总览

登录后进入主页，底部导航共 **5 个标签**：

| Tab | 内容 |
| --- | --- |
| 课表 | 周视图 / 日视图课程表（见第 4 章） |
| 学工 | 学工系统的日常请假与节假日去向登记（见第 7 章） |
| 成绩 | 学期 / 总体 / 考试成绩（见第 5 章） |
| 二课 | 活动中心、已报活动、申报记录、站内消息与成绩单（见第 6 章） |
| 我的 | 账号、外观与各项配置，校园服务（考级项目报名、一卡通、空闲教室）入口（见第 10 章） |

> 1.2.6 起底栏第 2 格由「选课」改为「学工系统」；选课功能已从本应用移除，需要选课请使用学校教务网页。

界面行为：

- **底栏自动收起**：页面向下滚动时玻璃底栏自动收缩隐藏，向上滚动或回到顶部时恢复，一屏能多看一节多的内容。想让它一直待在原地，可在「我的 → 外观 → 底部导航栏自动收起」里关掉（见 10.4）。
- **消息红点**：有未读教务消息时，「我的」页入口和底部导航会显示红点。
- **启动首屏**：可在「我的」中选择下次启动直接进入哪个页面（默认课表），见 10.5。
- **减少动效**：系统开启"减少动效"（无障碍）后，应用动画自动精简。

---

## 4. 课表

### 4.1 日视图与周视图

顶栏右侧有「日 / 周」分段开关，两种视图随时切换，各自记住浏览位置：

- **周视图**（默认）：网格为「时间列（节次号 + 起止时间）+ 周一至周日」7 列；课程卡片显示课程名与地点。没有周末课时可以在课表设置里把周末两列收起来，只画周一至周五 —— 五列比七列宽约四成，课名和地点都更好认（见 4.8）。
- **日视图**：按天翻页，一天的课程以卡片纵向排列，每张卡片带起止时间、节次、教室与教师；正在上的课标「正在上课」，接下来的一堂标「下一节」，顶部一行「今日 N 堂 · 还剩 M 堂」实时汇总；浏览别的日子时，时间列会出现「今天」按钮，一键回到今天。
- **同课程同色**：同一门课的所有卡片颜色一致（16 色调色板自动分配），不同课程不会撞色。
- 卡片上的状态图标：
  - ▶ 播放图标 = 这节课**正在上**（按节次时间实时刷新）；
  - ⚠ 警告图标 = 与其他课程**时间冲突**；
  - ✏ 铅笔图标 = 自定义添加的课程（补课生成的课程不显示铅笔）。
- 顶部标题显示「第 N 周」和今天的日期，浏览非本周时标注「(非本周)」。星期条下方显示每天的日期：本周高亮今天，非本周高亮周一作为锚点；日视图下高亮跟随正在浏览的那天，点星期条任意一天可直接跳过去。日视图的日期条上还有一个跟着你翻页走的滑块 —— 翻到哪天就圈出哪一格，不用低头数日期；今天那一格底下另有一个小圆点，翻到第几周都找得到今天（周视图一次看一整周，滑块不出现）。
- 右上角的「···」菜单收着课表的全部动作：同步课表、导出课表、添加课程、组件工作台、课表设置。

### 4.2 切换周次与回到今天

- 在课表上**整页左右横滑**翻周（日视图下横滑按天翻页），带缩放视差动画；第 1–25 周。
- 日视图下点星期条上的任意一天，跳到本周那一天。
- 偏离今天后，日视图的时间列会出现「今天」按钮，点一下回到当前周当天。

### 4.3 课程详情

点击任意课程卡片，从卡片位置弹出底部详情弹层：

- 上课时间、地点、教师、周次范围。
- **冲突提示**：说明与哪门课在第几周第几节重叠。
- **冲突选课（本节上哪门）**：当这节课与其它课程时间冲突时，详情弹层会列出全部候选（本节课 + 与它撞车的课）。点选要上的那门，另一门会在课表格子里让位隐藏。选择按"节"记录——两门课只有部分节次重叠时，只有真正重叠的那几节让位，其余时段互不影响；选择按账号保存，冷启动后仍生效。点「清除选择 · 显示全部冲突课程」可一键恢复全部显示。
- 补课 / 调休生成的课程会显示来源说明（对应平时星期几的课）。
- **课程提醒开关**：开启后按「提前提醒时间」提醒（默认上课前 15 分钟，可在提醒设置里改），并显示下次提醒的具体时间；权限不足时提供「设置提醒权限」引导，缺少学期时间时提供「设置学期时间」引导（详见第 11 章）。总开关关闭时这里会写明「暂不生效」。
- 自定义课程额外有「编辑课程」按钮和「更多课程操作」菜单（删除课程）。删除后有 **5 秒「撤销」** Snackbar，课程和提醒会一起恢复。

### 4.4 补课（调休）

当某天的课因为节假日调到周末时，用补课功能把课"搬"过来：

1. **入口**：点星期条上的「六 / 日」格子（不是按钮）。如果把周末两列收起来了（见 4.8），这两格不再显示，改从「课表设置 → 自定义课程 → 补课」进入。
2. 弹窗里有**四个滚轮**，按「补到第几周 → 补到哪一天 → 要补哪一周 → 要补哪一天」排列：先定这堂课要落到第几周周几，再选它原本是哪一周周几的课。下方实时预览「第 N 周 · 星期X 的 M 门课 → 第 N 周 周六」。
3. 点确认后，那一天的**全部课程按原时段**生成到目标格子；同一格重复补课会自动合并周次，不会叠加出重叠卡片。落点不在当前浏览的周时，课表会自动翻到那一周。
4. 目标格不能就是要补的那一格本身（同周同天同时段）—— 那会生成一条与原课程完全重叠、看得见却点不中的卡片，所以这种情况下确认按钮会置灰并标明原因。
5. 补课生成的课程默认不排提醒，但会生成一条提醒记录，可在课程详情里一键开启；它们同时也是自定义课程，可在课表设置里随时删除。

### 4.5 自定义课程

课表设置页可以添加不属于教务系统的课程（社团、自习等）：

- 字段：课程名称（必填）、教师（可选）、地点（可选）、星期（1=周一…7=周日）、开始节次、结束节次、周次（支持「1-16周(单),18周」这类写法）。
- 时间重叠时不会阻止保存，但会列出冲突课程提示「时间重叠，仍可保存」。
- 自定义课程按账号隔离保存，可随时编辑或删除。

### 4.6 导出课表

课表顶栏「··· → 导出课表」会先弹出格式选择：

| 格式 | 内容 |
| --- | --- |
| 日历文件（.ics） | 生成标准 iCal 文件并调起系统分享，接受方选择系统日历即可导入 |
| Excel 表格（.xlsx） | 生成 xlsx 并调起系统分享，列：课程名称、教师、地点、星期、开始节次、结束节次、周次 |

未设置学期开始日期时会先引导去设置；空课表无法导出。

### 4.7 从 Excel 导入课表

课表设置页 → 「从 Excel 导入」，走系统文件选择器挑一个 `.xlsx`：

- **列按表头名识别，顺序可以不一样**：「课程名称 / 课程 / 名称」「教师 / 老师」「地点 / 教室 / 位置」「星期 / 周几 / 礼拜」「开始节次 / 起始节次 / 开始」「结束节次 / 结束」「周次」都认。
- 「星期」接受 `周一`、`星期一`、`周1`、`1`、`Sunday` 等写法；「周次」接受 `1-16周`、`1,3,5`、`2-8双` 等写法。
- 表格没有表头时，按导出格式的列顺序（课程 / 教师 / 地点 / 星期 / 节次 / 周次）按位置取列。
- 导入的课程作为**自定义课程**出现在当前学期，可逐条修改或删除；重复课程会自动跳过，导入完成后会提示「已导入 N 门课程（跳过 M 门重复）」。

### 4.8 课表设置

课表顶栏「··· → 课表设置」进入课表设置页：

| 设置项 | 说明 |
| --- | --- |
| 添加 / 编辑 / 删除自定义课程 | 见 4.5；补课生成的课程也会出现在这个列表里，可删除 |
| 课程提醒 | 进入课程提醒设置页：总开关、提前时间、上课自动模式与四项权限自检（见第 11 章）；这一行会直接显示当前状态，如「已开启 · 15 分钟」 |
| 补课（仅隐藏周末时出现） | 把某天的课调到另一周的另一天，见 4.4；只在「显示周末」关掉时出现 —— 那时日期条上的六 / 日不再画出来，这里是补课的唯一入口 |
| 第一周开始日期 | 滚轮选择开学第一周的周一；副标题会显示当前生效日期是「已按你的设置」还是「按学期推算」（秋季默认 9 月 1 日、春季默认 3 月 1 日所在周的周一） |
| 每天节数 | 滚轮选择 8–16 节 |
| 显示密度 | 「课表显示」区里点选「标准 / 紧凑」；紧凑模式节次行高收窄，一屏能多看约四分之一的课程，课程名与地点仍完整显示 |
| 课表字号 | 小 / 标准 / 大 / 特大 四档（0.85–1.30 倍），点选即生效；只放大缩小课表文字，不改网格布局，按账号保存 |
| 显示周末 | 「课表显示」区的开关，默认**显示**；关掉后周视图只画周一至周五（五列比七列宽约四成）。日视图恒为 7 天 —— 周末的课不会消失，改由日视图承担展示（见 4.1） |
| 同步课表 | 从教务系统重新拉取当前学期课表（也可在课表顶栏「··· → 同步课表」直接点） |
| 从 Excel 导入 | 选择 .xlsx 表格，导入为可编辑的课程（见 4.7） |
| 节次时间 | 点任意一节弹出时间范围滚轮选择器，修改该节起止时间 |

> 小贴士：如果课表"日期不显示"或周次对不上，优先检查「第一周开始日期」——「我的」页副标题会告诉你当前用的是什么日期、来自哪里。

### 4.9 组件工作台与桌面组件（1.2.6 起）

课表「··· → 组件工作台」进去是一块可以自己摆布的信息页：把课表、考试、成绩、二课、消息里最常看的那几项各摆一张卡片，随手一瞥就知道接下来要做什么。同一批卡片还能直接放到手机桌面上（壁纸上面那一层），内容与应用里一模一样，点一下直接跳过去。

**七种卡片**（应用内与桌面共用同一份数据、同一套数字口径）：

| 卡片 | 内容 | 可用尺寸 |
| --- | --- | --- |
| 下一节课 | 只突出当前或接下来的一节 | 小 / 宽 |
| 今日课表 | 正在上的那节 + 紧接着的下一节，附今日课程数 | 小 / 宽 / 中 / 大 |
| 今日时间轴 | 今天全部课程按时间排开，进行中的那节高亮 | 中 / 大 |
| 考试倒计时 | 最近一场考试的天数、时间与考场座位 | 小 / 宽 / 中 |
| 成绩概览 | 平均绩点、已修学分与已出成绩门数 | 小 / 宽 / 中 |
| 第二课堂积分 | 各模块积分进度与全校参考值对比 | 小 / 宽 / 中 |
| 未读消息 | 教务消息未读条数与最近一条 | 小 / 宽 |

**在工作台里摆卡片**：

- 顶部「+」打开**组件抽屉**，按卡片逐条添加；抽屉里每条都能先看两样预览 —— 应用内的卡片效果，以及**桌面上最小尺寸的样子**（用真实渲染器出图），免得加完才发现桌面上少一行。
- 卡片按四列网格排列，**长按即可拖动换位**；每张卡片可改尺寸档位、移除，或「添加到桌面」。
- 排版是这台设备上的个人偏好；卡片里的内容则严格跟着当前登录的账号走。
- 「今日时间轴」在应用内和桌面上都带一条时间轨道，进行中的那节会亮起来。

**把卡片放到手机桌面**：

系统不同入口不同，请对照你的系统：

| 系统 | 怎么做 |
| --- | --- |
| 多数系统（原生 Android、小米、华为等） | 在工作台里点卡片上的「添加到桌面」，弹出确认框后点「添加」；也可以长按桌面空白处 →「小组件」，在列表里找「校园助理」 |
| OPPO / 一加 / realme（ColorOS） | 系统不会弹确认框，请走桌面自己的入口：**长按桌面空白处 →「卡片」→ 卡片中心 → 一直往下滑到最底部 →「插件」→ 选「校园助理」**，在列表里点要加的那张卡片即可落桌面 |

> ColorOS 的桌面长按菜单是「卡片 / 壁纸 / 图标 / 布局 / 翻页 / 更多」，**没有「小组件」这一项**；标准微件列表被收在「卡片中心」列表最底部那条「插件」里，往下滑到底就能看到。组件本身能正常添加，并显示真实数据。

**其它行为**：

- 桌面组件读取**本地缓存**，不打开应用也能看；登录状态、课表同步、消息变化、每到一节的上下课时间都会自动刷新（系统另有最长 30 分钟的兜底周期）。
- 随系统浅色 / 深色模式自动切换配色。
- 点卡片进入对应页面：课表类卡片会**复位到本周今天**；卡片上的课程行点一下直达那门课的详情（课程已不在课表里时退回课表页）。
- 组件按当前登录账号显示，切换账号后内容跟随变化。
- 1.2.2 曾移除旧版课表小组件，1.2.5 回归三种样式，1.2.6 起统一为上面这七种卡片。桌面上的旧组件长按删掉、重新添加新的即可，与旧版数据无关。

---

## 5. 成绩与考试

成绩 Tab 有三个子页：**学期 / 总体 / 考试**（玻璃分段条，上划收拢为悬浮条，各页独立记忆滚动位置）。

### 5.1 学期成绩

- 顶部学期选择器：近 4 学年的全部学期，最新在前。
- 统计条：**平均绩点（按学分加权自动计算）、总学分、课程数**。
- 课程列表：课程名 + 课程代码/开课学院，右侧学分和大号成绩；成绩按分数段着色（90+ 绿、80+ 蓝、70+ 橙、60+ 主体色、不及格红）。
- **点击课程卡片展开**：平时 / 期中 / 期末分项进度条（缺失的期末分会按总评自动反推）、绩点 / 学分 / 课程性质 / 教学班信息芯片、备注。

### 5.2 总体成绩

- 统计条：累计绩点、已修学分、总课程数。
- **成绩分布卡**：优 / 良 / 中 / 及各分段彩色条形与计数。
- 全部课程明细，同样可点击展开。

### 5.3 考试安排

- 考试数统计条 + 按时间排列的考试卡片：考试名称（期中标橙色）、考试时间、地点 + 座位号、教师。

### 5.4 导出成绩单

学期 / 总体页顶栏的分享按钮可导出 **Excel（.xlsx）成绩单**，表头加粗，列：学年、学期、课程名称、课程代码、开课学院、学分、成绩、成绩分项；导出后调起系统分享，Excel / WPS 都能直接打开、不会乱码。

> 加载失败时页面会给出「重试」按钮，不用自己去找顶栏的刷新。

---

## 6. 第二课堂

底栏第 4 个 Tab 是第二课堂（到梦空间 / 二课平台），页面大标题为「**第二课堂**」。页面顶部有 **5 个分段**：**活动 / 已报 / 申报 / 消息 / 成绩单** —— 报名活动、看自己报过的活动与已提交的申报、看站内消息、查积分与排行榜，都在这一页里完成。活动页顶栏只有一个「···」菜单：**筛选活动 / 刷新 / 扫码签到**都排在里面。

### 6.1 登录

- 学校接入二课后，首次进入会引导「登录第二课堂」。
- 账号固定为教务学号（只读），**密码默认预填学校初始密码：学号 + `&Zhtx`**，改过密码可直接覆盖输入。
- 登录成功后保存凭证（系统密钥库加密、仅存本机），失效时自动续期；已保存密码后，登录弹窗变为「删除已保存的第二课堂密码」。
- 活动报名与奖励申报已可在应用内直接提交（见 6.4）；销假、审批等仍以官方站点为准。

### 6.2 成绩单与积分

「成绩单」是这一页最右侧的 Tab（在「消息」右边）：

- 头部显示姓名、学号/年级、学院·专业，以及三个统计：**学分、总积分、综测附加分**。
- **综测附加分**是学生综合素质测评里的附加分项，由各模块积分按下表权重**加权求和**得出（单项封顶 100 分，满分 100 分），站点本身不返回这个值，由应用本地算出：
  - 思想政治素养 0.15 ｜ 社会责任担当 0.15 ｜ 实践实习能力 0.10 ｜ **创新创业能力 0.30** ｜ 文体素质拓展 0.15 ｜ 菁英成长履历 0.10 ｜ 技能培训认证 0.05
  - 学校没记录的模块按 0 分计入；若一个模块都取不到，这个框**不会显示**（避免给出错误的 0 分）。
- - 「各模块积分状况」逐行显示每个模块的我的积分 / 学校要求值，进度条按完成度着色（未达标红 / 接近主题色 / 达标绿），并附「全校参考」值。
- **分类与学期统计（三层对账）**：成绩单往下还有两段 ——「**按分类统计**」与「**按学期统计**」。
  - 两段都先给一行**小计**（记录条数 / 学时），再逐条列出明细；明细可展开收起。
  - 目标是「**每一分有迹可循**」：应用会把「总学时 → 两维小计 → 逐条明细」三层对起来核，核对结果以一枚**对账标记**直接显示出来 —— **已对平 / 明细不全 / 对不上 / 未核对**四种状态。对不上时会同时给出差额出现在哪个维度，避免你只看到"数字不一样"却不知道差在哪。
  - **量纲说明（很容易看错）**：对账行里的数字一律是**学时**，不是折算积分。成绩单顶部的「总积分」是折算后的分值（例如 5.16），与学时（例如 25.8）本就是两个量纲，不能拿来互相印证 —— 所以这两处刻意不放在同一行比较，折算积分单独一行并标注。
  - 「参加了多少类事」按**名称**去重统计；「多少笔账」另有一个**记录条数**。两者口径不同，不要混用。
  - 这两个接口一次返回全部数据、**不支持分页**，也不需要按学期切换 —— 列表本身已按分类 / 学期分好组。

- 这一 Tab 按需加载：不点开就不发成绩单与排行榜的请求，活动中心可以更快打开。

### 6.3 排行榜

- 四级分段切换：**班级 / 专业 / 院系 / 全校**；顶部有「我的排名」卡片（第 N 名或未入榜 + 分数）。
- 班级层级展示完整榜单：前三名金银铜名次徽章、姓名、专业、分数。
- 专业 / 院系 / 全校层级只显示自己的名次。
- 名次按积分重新计算，**同分并列共享名次、不跳号**。
- 隐私：榜单旁的「显示班级同学排名」开关关闭后，班级榜单隐藏同学名单，只保留我的排名。

### 6.4 活动列表与「已报」

- 「活动」Tab 列出全部活动，支持关键词搜索、分类筛选与排序；「··· → 筛选活动」弹窗还能只看「未报满」「本院系可报」。
- 活动卡片显示封面、名称、时间、地点、名额进度与主办方；点进详情可以看到完整介绍、附件与「我的报名状态」。
- **名额报满**的活动，卡片上会直接标出「已满」；详情页的报名按钮会变成不可点的「报名人数已满」。同理，报名没开始显示「报名未开始」，报名已截止显示「报名已截止」。
- 「本院系可报」按**活动自己的院系限制**判定：对全校开放的活动一律保留，只在活动明确限制了其它院系时才隐藏。你所在的院系由第二课堂账号自动带出，筛选弹窗里会写出院系名。若还没绑定第二课堂（取不到院系），这一条会暂时全部放行，不会把列表筛空。
- **报名已经截止的活动不会出现在列表里**：报名窗口关了就报不了名，留在列表上只会误导。列表状态行会说明隐藏了几条；如果一整批活动都被这一条过滤掉，还会给出「继续加载」，不至于卡在一张空页上。
- 还没开始报名的活动**仍然保留**，点进详情会看到禁用的「报名未开始」。
- **加载失败不会假装「没有活动」**：凭据失效或过期时给出「去绑定」，网络不通时给出「重试」。在活动中心里点「去绑定」，登录成功后列表会自动重新拉起来，不会卡在「尚未绑定第二课堂」上。
- 报名需要填写自定义信息（姓名、手机号之类）时，会在弹窗里逐项填写；取消报名会先弹确认框。
- 「已报」Tab 汇集自己报名过的活动，顶部一张汇总卡显示已获得学时与已报名数量。这一栏**不受**上面的截止过滤影响 —— 报过的活动即使报名已截止，也照样能在这里翻到。
- 「消息」Tab 接收活动通知，有未读时 Tab 上会显示红点。
- **「申报」Tab**（在「已报」右边）就是奖励申报入口：顶部分段切「**未申报 / 已申报**」，先选**类型**（类别）与**分类**筛选可申报的项目，列表里每项显示项目名、所属模块与「共 N 个奖项」。
- **申报提交**：点项目进入填报页，依次选择**奖项名称**（档位）、项目起止日期、填写**总结报告**、上传**证明材料**（必传，可关联活动），确认后提交。提交后等待审核，结果以第二课堂站点为准。
- 「已申报」分段列出你已提交的申报：项目名、认定档位、所属模块、认定学时与时间；点开看完整字段。
- 申报条目的状态按站点返回的**原始状态码**展示（例如「状态 2」）。站点没有下发状态文案，应用不把它翻译成「已通过 / 已驳回」这类结论，以免误导。

### 6.5 扫码签到

活动详情页的「签到」一栏是唯一的签到入口（独立的「签到记录」标签页在 1.2.3 已取消，历史记录请到活动详情页查看）：

- **扫码签到**：点顶栏「··· → 扫码签到」，弹窗里二选一 ——「相机扫码」对着签到员出示的二维码扫一下，或「从相册识别」选一张图片。
- **从相册识别**：适合同学把签到码截图发过来的场景，不用拿另一台手机对着屏幕拍。自动处理大图采样、EXIF 方向，拍歪的、倒着的、背景花的都能识别。
- **我的签到码**：在活动详情页出示自己的动态二维码，让签到员扫。
- 相机扫码页**固定为竖屏**，不会被系统旋转打断。
- **防误操作拦截**：扫到自己的等待码、未报名的活动、组织码，或者在不是签到员时替他人签到，都会被拦下并说明原因；在列表页扫码成功后也会有明确提示。

### 6.6 扫码直达

除了在应用里手动点，第二课堂的活动与签到还有两条「直达」路径：

- 用**系统相机**扫第二课堂的签到码或活动页链接，应用会提示是否直接跳过去。
- 把链接**复制到剪贴板**后打开应用，也会提示是否跳转。
- 跳转目标：活动码 → 活动详情页；签到码 → 签到确认流程。

扫到活动码是「看活动」，不会进签到闸门；只有真正的签到码才会走签到安全判定。

### 6.7 活动附件

活动详情与教务通知里的附件（`attachment[].url`）可以直接在应用内打开，不用跳出去：

- **支持**：Word（`.docx`，标题 / 段落 / 列表 / 表格 / 图片 / 分页）、Excel（`.xlsx` / `.csv`，多工作表切换、冻结表头、点单元格看全文）、PDF（内置 `PdfRenderer`，双击缩放）、图片（捏合缩放 + EXIF 方向）、文本（BOM / UTF-8 / GBK 自动识别）。
- **不支持**：旧版 `.doc` / `.xls` —— 它们是二进制格式，会提示用 WPS / Office 打开，也可以用「用其它应用打开」或分享。
- 单文件下载上限 25MB；附件直链不需要登录态，因此下载不带认证信息。

---

## 7. 学工系统

1.2.6 起学工系统是底栏第 2 个 Tab，点「学工」直接进入（此前在「我的 → 校园服务」里）。学工系统（`xg.hnnu.edu.cn`）是学校**学生工作处**的系统，**日常请假**与**节假日去向登记**都在这里办理。

> 这是与教务、第二课堂都不同的第三套系统。账号是**教务学号**（只读带入），密码通常也是学校统一发的那一个 —— 但它与教务密码不一定相同。

### 7.1 登录

- 账号固定为教务学号，只需输入学工系统密码；登录后密码经系统密钥库加密后仅存在本机。
- 登录状态（24 小时）失效时会用保存的密码自动重连一次，失败才回到登录框。
- 顶栏右侧两个按钮：**刷新**与**退出学工登录**。退出只清本机的学工凭据，不影响教务与第二课堂的登录状态。

### 7.2 日常请假

- 顶部状态卡写明校方当前**是否开放申请**，以及请假是否要求**定位打卡**（要求时会写出允许的偏差距离）。校方开放申请时，状态卡上会出现「**写请假**」按钮。
- 点「写请假」进入填表页：请假原因（下拉）、开始 / 结束时间（滚轮选择，自动按整点计）、详细说明、是否离校、离校时的外出时间与交通方式、监护人告知情况、同行情况等，字段与学工官方页面一致。证明材料可从相册添加图片（单张不超过 8M）。
- 点「提交」前应用会做与官方页面相同的必填校验，并弹出确认框；提交后由校方审批，结果以学工系统为准。
- 下方逐条列出你提交过的请假：类型、审批状态、起止时间、天数与事由；点开看完整字段。
- 没有记录时会明确说明"还没有提交过日常请假"，而不是一张空白页。

### 7.3 节假日去向登记

- 「登记批次」就是主列表：每个批次一行，显示假期时间、登记起止时间与校方状态。
  - 批次**已登记**：点开直接查看登记详情（去向类型、离开与返回时间、去向、交通方式、家庭所在地、本人手机与紧急联系人等完整字段）。
  - 批次**开放中且未登记**：点开进入填表页，选「离校 / 留校」后填写对应的时间、去向、交通方式与联系信息，确认后提交。
  - 批次**未开放**：如实显示校方状态，点击会提示暂未开放。
- 提交后回到列表即可看到最新状态；登记开放期内可再次进入修改重新提交，以学工系统为准。

### 7.4 提交范围说明

本应用提供**日常请假**与**节假日去向登记**的填写提交，序列化与官方页面完全一致；但**销假、审批、撤销**等仍需在学工系统官方页面办理，页面底部也写明了这一点。

个别字段（例如请假审批状态）以**原始状态码**展示：学工系统没有下发状态文案，应用不替站点下结论；要看审核意见请到官方页面。


## 8. 考级项目报名

从「我的 → 校园服务 → 考级项目报名」进入。这是教务系统里的等级考试报名模块（四六级、普通话等），复用教务登录态，进入即可用，无需二次登录。

### 8.1 报名

- 列出当前开放的考试批次：名称、类别、费用、名额与剩余名额、报名起止时间与报名说明。
- 点「报名」先在确认弹窗里复核报名说明并填写联系电话，确认后提交；提交前的必填校验与官方页面一致。
- 名额已满、不在报名窗口内等情形以教务系统的判定为准，应用不会绕过学校的报名限制。
- **类别可以切换**：同一个入口下，学校可能开有多个报名类别（如「等级考试」「参加大类分流报名」「推免报名」）。查到多个时页面顶部会出现一排类别切换条，点一下即换；只有一个类别时不显示这条，界面与往常一致。类别清单取自教务系统自己的功能菜单，不是应用里写死的。
- 当前没有开放批次时，这里直接说明原因（优先显示教务系统自己写的提示语），不会再被当成「读取失败」。
- **本学期过期项目**：页面底部有一块「本学期过期项目」，列出本学期已经截止报名的批次（名称、学期、报名起止时间与费用）。点一下才去查，不会因为进入页面就多等一次；切换类别后会重新查。

### 8.2 已报名记录与退报

- 已报名记录汇总在同一页：学期、报名时间、费用、缴费状态、准考证号、成绩与证书编号 —— 出了成绩、发了证书都能直接在这里看。
- **缴费状态**直接标在记录上：「已缴」或「未缴」。用的是教务系统自己的说法。**这一行空着不等于「未缴」** —— 那是"该项目不涉及缴费"，别当成待办。
- **审核状态**只在异常时才占一行（待审核 / 审核中 / 退回 / 不通过）；「已通过」是常态，不会逐条显示。审核未通过的记录在教务侧同样不允许缴费。
- **退报**只在批次仍开放且未过报名截止时间时提供，按钮消失时会写明**具体**原因，而不是一律说"过了截止时间"：
  - 已缴费，不支持退报
  - 该批次已不在本学期开放列表中，不支持退报（往期学期的记录）
  - 已过报名截止时间，不支持退报
- 已缴费的项目在列表里就**不再给出退报按钮**，不必点进去、确认之后才被告知不能退；「正在缴费」这一状态教务只在提交时才判，仍以提交结果为准。
- **一次最多显示 15 条**：教务系统这个接口不接受任何分页参数，固定只返回 15 条。超出时页面会注明「这里显示的是最近 N 条（共 M 条）」，更早的记录请到教务系统网页端查看。


## 9. 消息中心

从「我的」页「消息中心」进入（有未读时入口带红点和数量）。

- **未读 / 已读分组**：顶部玻璃分段切换，标签上直接显示条数（如「未读 3」），默认停在未读。
- 消息行：未读蓝点、标题加粗、发送者与发送时间、两行摘要；组内按时间倒序。
- **点开即已读**：点击消息乐观标记已读，全局未读数当帧刷新（红点同步消失）；详情页展示完整正文，列表已带正文的消息就地展示不再二次请求。
- **一键已读**：顶栏的「一键已读」按钮常显，有未读时点一下即全部标为已读、红点立刻消失，并有完成提示；没有未读时点击只会提示一句，不打扰。
- 在应用里标过的已读状态，刷新后与服务端结果合并保留，不会被打回未读。
- 离线优先：先展示缓存再刷新；错误态可「在教务网页查看」跳内嵌浏览器。
- **新消息通知**：应用回到前台时按 30 分钟节流检查教务消息，有新未读时发系统通知（每 12 小时也会后台巡检一次）；设备重启、应用更新后自动重排巡检。

---

## 10. 「我的」页逐项说明

「我的」页 各分组与条目如下。

### 10.1 页头

- **头像**：点击可从相册选图，进入方形取景框裁剪页（支持双指缩放、拖动、自动摆正旋转），保存后立即刷新。
- 姓名，下面是**你所在的院系与专业**（如「金融与数学学院 · 大数据管理与应用」）；院系/专业由第二课堂账号带出，还没绑定第二课堂时这一行退回显示学校名。
- 「超级用户」等身份徽标（如有）。
- **更新 Cookie**：手动刷新当前登录状态（密码登录账号可用），成功/失败有提示。

### 10.2 账号与教务

| 条目 | 说明 |
| --- | --- |
| 账号管理 | 查看、切换、删除密码 / 删除账号；空槽位即「登录其它账号」（见 2.4） |
| 消息中心 | 进入消息中心，副标题显示未读数 |
| 公告 | 打开公告中心整页（未读 / 已读分段），有未读时带红点 |
| 通知提醒 | 显示系统通知权限状态；关闭时点击直达系统通知设置（成绩发布、站内消息与考前提醒都依赖它） |

> 第二课堂不在「我的」页：它是底栏第 4 个 Tab，成绩单是那一页最右侧的 Tab（见第 6 章）。

### 10.3 校园服务

| 条目 | 说明 |
| --- | --- |
| 一卡通 | 校园卡余额、宿舍电费与消费汇总（**只读**，不提供充值/缴费）。登录走学校统一身份认证，本应用不保存一卡通密码。详见 10.3.1 |
| 空闲教室 | 教务系统的空闲场地查询：按校区 + 周次 / 星期 / 节次找没课的教室（**只读**，不提供场地借用申请）。详见 10.3.2 |
| 考级项目报名 | 教务系统的等级考试报名（四六级、普通话等）：报名、退报与已报名记录，详见第 8 章 |

> 学工系统不在这一组：1.2.6 起它是底栏第 2 个 Tab（见第 7 章）。

#### 10.3.1 一卡通（校园卡）

从「我的 → 校园服务 → 一卡通」进入。首次进入会要求登录 —— 点「去登录」在页面内用学校的**统一身份认证**走一遍即可，登录状态会保存在本机，之后进出不必重复登录。**本应用全程不保存你的一卡通密码，也不提供任何充值、缴费功能**。

**余额与卡面信息**

- 顶部一张卡片给「**账户总额**」——按学校配置的口径合计，与校园卡官方页面一致；下方同一张卡里直接列出卡号、账号、有效期与电费账户，不用再在两处之间对照。
- 卡状态（正常 / 挂失等）以徽章形式展示，便于一眼判断卡是否正常；已挂失时另有一枚醒目的「已挂失」徽章。

**挂失 / 解挂校园卡**

- 卡片底部给出「挂失校园卡」按钮（已挂失的卡则显示「解挂校园卡」）。**挂失是不可逆的破坏性操作**，所以按钮点下去不会立即执行，而是弹出**两段式确认**：
  1. 第一段把后果说清楚（卡立即冻结、不能再消费、补卡要去卡务中心），按钮只是翻页，不会执行任何操作；
  2. 第二段要求你**手动逐字打出**一句话 —— 挂失时是「我确认挂失这张校园卡」，解挂时是「我确认解挂这张校园卡」 —— 并要求输入一卡通查询密码。文字完全一致且密码非空时，确认按钮才会点亮。
- 要求手打一整句而不是"再点一次确定"，是为了挡住手滑连点与自动化脚本：你必须真实地打一遍这句话，而这句话本身就在陈述你要做的事与它的后果。
- 输入时的**空格、换行、全角/半角差异会被忽略**（这些只是格式），但**内容必须一致** —— 少一个字、换个近义词、加个标点都不通过。挂失与解挂两句话不同，不能互相替代。
- 保护你的一卡通密码：**密码只存在于这次弹窗里**，随弹窗关闭即丢弃，**本应用不会存储它**（与登录一样，代码里不存在保存一卡通密码的接口，由单测护栏守住）。
- 操作结果以服务端返回为准：成功后卡片状态会重新向学校服务器拉取，**不做本地乐观更新** —— 否则服务端没收到请求时，界面会显示"已挂失"而卡其实还能刷，这种不一致比"状态没变"危险得多。

**宿舍电费**

- 先在「电费项目」里选对那一套（例如「1-6单元电控缴费」与「7-11单元及东区电控缴费」），再依次选 **校区 → 楼栋 → 楼层 → 房间**：电费**按房间计量**，选完房间才会出数。选过的房间会被记住，下次进来直接出数；换了费项或楼栋，下游的楼层 / 房间会自动清空，避免拿旧房间号去查。
- 房间不是学校绑定的，是**你自己选的** —— 想查哪一间就选哪一间（同宿舍、隔壁寝室都能查）。
- 需要给房间充值缴费时，点「在官方页面查看/缴纳电费」，由学校的官方页面处理。
- 有的费项返回的是**电量（度）**而不是金额，界面会明确标出「度」并说明它不参与金额提醒 —— 拿"312 度"去套"低于 20 元"是说不通的。
- 若接口返回但识别不出数值，界面显示「暂时无法获取」而不是 0，不会把查询失败伪装成"没电了"。

**低余额提醒**

- 默认电费低于 **20 元**时提醒，阈值可在页面内的「余额提醒」里自行修改。
- **不限提醒次数**：只要余额低于阈值就会提醒（后台每日巡检一次，所以最坏情况是每天一条，不会刷屏）。通知栏里始终只留最新一条余额，不会堆成一片。
- 提醒需要本机已经登录过一卡通（后台巡检用本地令牌查一次电费）；从没登录过时提醒不会生效，这是刻意不做静默登录的结果。
- **建议允许后台运行**：提醒页面里有「电池优化白名单」与「自启动设置」两个入口。被系统限制了后台运行的话，提醒会时有时无 —— 把这两项都设好最可靠（不同品牌入口名称不同，如「自启动 / 后台运行管理 / 省电策略」）。

**消费汇总与逐笔明细**

- **月度汇总**：按月展示校园卡的**支出**与**收入**两行数字，左右箭头切换本月 / 上月 / 更早。
- **逐笔明细（全部）**：可折叠列表，点标题展开 / 收起。注意这里是**全部历史流水**，不是本月 —— 账单接口本身不支持按月筛选（官方页面同样是拉全量再展示），所以别把明细的头几笔当成"本月花销"。
- 页面底部另有一个「在官方页面查看（退款 / 导出 / 标签）」入口：这些操作只有校方页面有。

#### 10.3.2 空闲教室（空教室查询）

从「我的 → 校园服务 → 空闲教室」进入。这是教务系统里的**空闲场地查询**（正方 `cdjy/cdjy_cxKxcdlb.html`），复用教务登录态，进入即可用，无需二次登录。

**怎么查**

- 选 **校区**（必选）→ 可再选 **楼号**、**场地类别**、**座位数区间**、**场地名称关键词**（都可不选，表示不限）。**楼号与节次的候选随校区变化**：换校区后会重新从学校取该校区可用的楼栋与节次，已选的楼号与节次会被清空（避免拿 A 校区的楼号去查 B 校区）。
- 选 **周次** + **星期** + **节次**：这三项各至少选一项，回答的是"第几周、星期几、第几节哪里空着"。周次和节次都是**可多选**的，多选时按"这些时段全都空着"来筛。
- 点「查询空闲教室」出结果。结果按场地编号排序，每行给出场地名称、楼栋 · 楼层 · 类别与座位数。

**分页**

- 一次最多取 100 条，结果末尾有「加载更多（已显示 N 间）」，标题右侧显示命中总数（如「共 537 间」）。
- 加载更多时如果学校又返回了同一批数据，会提示「教务系统没有返回更多结果」，而**不会**把界面停在"共 537 间却只列出 100 间"这种看起来正常的状态上。

**边界**

- 只做**查询**。网页上的「场地借用申请」是向学校提交写请求，本应用不代提交 —— 要借用场地请走教务系统网页。
- 「场地二级类别」筛选项未提供（它的候选由学校服务器按一级类别动态下发，尚未适配）；一级的「场地类别」已足够筛出可用教室。
- **楼号与节次按校区取**：换校区后会重新向学校请求该校区的楼栋与节次列表。若某个校区取不到楼栋列表，楼号只保留「全部」，此时可直接留空表示"不限"，或先选场地类别来缩小范围。

### 10.4 外观

| 条目 | 说明 |
| --- | --- |
| 主题 | 浅色 / 暗色 / 跟随系统，切换保留背景设置 |
| 背景 | 三页分段弹窗：**预设**（多款预设壁纸单选）；**颜色**（色相/饱和度/明度滑杆 + 12 枚推荐色 + 实时预览）；**图片**（相册选图 → 按屏幕比例取景裁剪，可再调「蒙版」「模糊」两根滑杆）。全部实时预览生效，图片仅存本机 |
| 字体 | **系统字体**（默认）/ **苹方字体**（随包内置，iOS 同款观感）/ **自定义字体**：点「导入字体文件…」从手机存储选一个 ttf / otf 文件，校验通过后立即应用；再次导入会覆盖旧字体。选项弹窗里每档都有实时预览，全文切换当帧生效 |
| 液态玻璃 | 开关；开启折射、色散与跟手形变特效，关闭后改用不透明材质，更省电、文字更清晰 |
| 底部导航栏自动收起 | 开关，默认**开**：页面向下滚动时收起底栏、向上滚动时展开。关掉后底栏始终待在原地，翻页时不再上下动 |

### 10.5 应用与支持

| 条目 | 说明 |
| --- | --- |
| 启动首屏 | 选择下次启动直接进入的页面：课表 / 学工 / 成绩 / 二课 / 我的 |
| 检查更新 | 从 Gitee 获取最新版本与更新说明。发现新版本时弹出液态玻璃提示，点「立即更新」即在**应用内下载**并唤起系统安装器，不再跳转浏览器；也可以选「以后再说」。应用每 6 小时自动查一次；退到后台后由系统闹钟每天再查一次，发现新版本直接发一条系统通知，点通知就能看更新说明 |
| 公告 | 打开公告中心整页（与消息中心同构）：顶部「未读 / 已读」分段，点条目即读。公告随应用内置、按版本下发 —— 只对**适用于当前版本**的公告计未读、弹启动提示；写给旧版本的历史公告会标成「历史」放在已读一栏，随时可回看 |
| 用户手册 | 应用内阅读本手册，离线可用，主题跟随应用设置 |
| 推荐网站 | 四六级报名、学信网等常用入口，点击用系统浏览器打开 |
| 分享给校友 | 把项目链接分享给需要的同学 |
| 导出日志 | 导出本地运行日志并通过系统分享（排查问题时用；日志已做脱敏，不含密码与 Cookie 值） |

### 10.6 支持作者

| 条目 | 说明 |
| --- | --- |
| 为本项目点 Star | GitHub · Elisoar111/hnnu-jiaowu-app |
| 为原作者项目点 Star | GitHub · znjhahaha/zhengfang-apk |
| 联系开发者 | GitHub Issue 或邮件反馈问题与建议 |
| 关于项目 | 项目介绍、开源地址、对原作者的致谢与反馈方式 |

### 10.7 数据与安全

| 条目 | 说明 |
| --- | --- |
| 清除缓存 | 确认后清除所有本地缓存数据释放空间 |

### 10.8 其他

- **退出登录**（红色独立条目，有确认弹窗）。
- 页脚显示版本号「校园助理 · x.y.z」。
- 应用当前仅支持淮南师范学院，学校条目点击会提示。

---

## 11. 课程提醒

每门课都可以单独设置上课提醒；提醒的**总开关、提前时间、上课自动模式与权限自检**集中在「课表设置 → 课程提醒」这一页。

### 11.1 单门课程的提醒

- **入口**：课表 → 课程详情弹层 → 「课程提醒」开关。
- 通知内容：「课程名 · 即将上课」，正文为地点 + 第 N-N 节；**点击通知直达课表页并打开该课详情**。
- 提醒基于系统精确闹钟，应用被杀也能响；**自对账机制**：切换账号、同步课表、编辑/删除课程、开机、系统时间变化后会自动重排或取消过期闹钟，删除自定义课程时提醒一并删除。

### 11.2 课程提醒设置

从「课表设置 → 课程提醒」进入（那一行会直接显示当前状态，例如「已开启 · 15 分钟」）。

| 设置项 | 说明 |
| --- | --- |
| 课程提醒（总开关） | 关闭后不再排任何上课提醒，逐门课程里的开关也一并失效；课程详情页会写明「总开关已关闭，这里的开关暂不生效」 |
| 提前提醒时间 | 上课时 / 提前 5、10、15、20、30、45、60 分钟，默认 **15 分钟**；改完已设的提醒会立即按新时间重排 |
| 上课自动模式 | 上课时自动把手机切到勿扰档位，下课时还原成你原来的设置。可选：关闭 / 勿扰（完全静默）/ 仅闹钟（只放行闹钟）/ 仅优先（只放行优先打扰，需先在系统里设好优先联系人） |
| 通知权限 | 没有它一条提醒都发不出来 |
| 精确闹钟权限 | Android 12 起必须授予，否则闹钟排不出来 |
| 勿扰模式权限 | 上课自动模式需要；没有它应用不会去切勿扰 |
| 后台运行和自启 | 部分 ROM 需要在应用详情页里单独允许 |
| 忽略电池优化 | 被省电策略限制时闹钟会被推迟甚至吞掉 |

> 权限自检里的每一行都能直接点开对应的系统设置页。**提醒不准时或完全不响，先来这里看一眼** —— 九成是这四项里有一项没开。

> 上课自动模式需要「课程提醒」总开关是打开的，也需要勿扰模式权限；条件不满足时页面会直接说明，并且**不会**去动系统设置（没有权限就施加，意味着还原不了）。

---

## 12. 通用操作与使用技巧

- **长按**：组件工作台里长按卡片可拖动换位；课表补课入口是点日期条的「六 / 日」（把周末两列收起来时，改从「课表设置 → 补课」进，见 4.4）。
- **下拉刷新**：成绩、消息中心等列表页支持下拉刷新，加载中有骨架屏。
- **撤销**：删除自定义课程后有 5 秒撤销窗口。
- **点击展开**：成绩卡片点击展开分项成绩；组件工作台里的卡片点一下跳到对应页面。
- **通知直达**：课程提醒通知点击直达对应课程详情；课程提醒失效时会明确提示。
- **扫码直达**：用系统相机扫第二课堂的签到码或活动链接、或把链接复制到剪贴板后打开应用，确认后直达对应活动或签到（见 6.6）。
- **自动恢复**：登录态失效自动提示并可在恢复后回到原页面；课表同步失败时保留本地数据。
- **账号隔离**：课表缓存、自定义课程、提醒、消息未读、二课凭证全部按账号隔离。

---

## 13. 数据与隐私安全

- **密码存储**：账号密码经 Android KeyStore 的 AES/GCM 加密后仅保存在本机，加密或读取失败一律按"未保存凭据"处理，不会明文落盘。
- **不上传隐私**：应用没有使用统计、没有行为上报；不收集学校、学号、课程内容或设备信息。
- **云备份收紧**：账号密码密文与会话 Cookie 文件已从系统云备份和设备迁移中排除，不会随备份扩散到其他设备。
- **日志脱敏**：运行日志只记录 Cookie 名，不记录 Cookie 值和明文密码，可放心导出分享。
- **网络严格校验**：HTTPS 证书走系统信任链严格校验；明文 HTTP 仅对备用 IP 入口放开。
- **图片本地处理**：头像与壁纸仅在本地裁剪、压缩和存储，不上传。
- **一卡通不碰密码**：一卡通（校园卡）只用学校统一身份认证，登录完全在官方页面完成，应用拿到的只是服务端下发的会话令牌；**代码里不存在保存一卡通密码的接口**（由单测护栏守住）。挂失 / 解挂时如果学校要求密码，会临时向你索取、用完即弃，同样不落盘。一卡通只做余额与电费查询，**不代收、不处理任何支付**；逐笔账单明细也只在官方页面渲染，应用不去抓取。唯一的写操作是**挂失 / 解挂**（你亲手触发、两段确认后才发出），因为它是保住余额的应急手段 —— 与此对应，**充值、缴费、转账一律不提供**。

---

## 14. 常见问题 FAQ

**Q1：登录不上怎么办？**
先点登录页的「测试站点连通性」：主站和备用入口都通 → 检查账号密码或验证码；主站不通、备用通 → 切换到备用入口（IP 直连）再登录；两个都不通 → 多半是校园网/学校服务问题，稍后再试。密码登录始终不行时，可用「Cookie 登录」或内嵌「网页登录」绕过客户端登录流程。

**Q2：课程提醒不响？**
按顺序检查：① 通知权限是否允许；② 是否授予「精确闹钟/闹钟和提醒」权限；③ 课表设置里「第一周开始日期」是否正确；④ 该课在本学期是否还有后续课次。详情页的提醒开关下方会写明当前缺哪一项。

**Q3：课表日期或周次对不上？**
去课表设置检查「第一周开始日期」。副标题会显示当前生效日期及来源（"已按你的设置"或"按学期推算"），手动设置一次即可纠正。

**Q4：二课密码是什么？**
学校初始密码为「学号 + `&Zhtx`」，登录框已自动预填；改过密码就输入新密码。应用只做查询，报名请回官方平台。

**Q9：怎么换图标头像 / 换背景 / 换字体？**
「我的」页点头像 → 相册选图 → 取景框裁剪；背景在 「我的」→ 外观 → 背景，支持预设、纯色、图片三种来源，图片可调模糊与蒙版；字体在 「我的」→ 外观 → 字体，可切内置苹方或导入手机里的 ttf / otf 文件。

**Q10：有新版本吗？**
应用每 6 小时自动检查一次，有新版本就弹出液态玻璃提示；退到后台之后也有一天一次的巡检，发现新版本会直接发一条系统通知，点通知即可查看更新说明并下载。也可以随时在「我的」→「检查更新」手动检查。两种方式都能直接点「立即更新」，由应用自己把安装包下载下来并唤起系统安装器 —— 不经过浏览器，也就不会遇到"下载下来是个压缩包"的问题。当前版本号显示在「我的」页页脚。若系统提示需要允许「安装未知应用」，按提示在设置里为本应用打开即可。后台巡检依赖系统通知权限，若关掉了通知，就只剩打开应用时的检查。

**Q11：为什么升级后要重新登录，课表也没了？**
从 1.2.2 起应用更换了安装包名（见 1.1 的包名说明）。安卓把包名当作应用身份，新旧包名是两个互不相干的应用，所以本地缓存的账号与课表不会跟过来。重新登录教务账号后同步一次，课表、成绩等数据都会重新拉取回来。

**Q12：遇到 Bug 怎么反馈？**
可通过「我的」→ 导出日志获取运行日志（已脱敏），连同问题描述发送给作者邮箱（「我的」→ 联系开发者）。
如果进不去 GitHub、没法在上面提 issue，欢迎加入 QQ 频道 **pd32446534** 反馈问题或提需求。

**Q13：桌面上的课表小组件怎么不见了 / 加不上？**
1.2.2 曾把旧版小组件全部移除，1.2.1 及更早版本添加的组件会显示成空占位，长按删掉即可。1.2.5 起回归、1.2.6 起统一为七种卡片（见 4.9）。添加步骤：**长按桌面空白处 →「卡片」→ 卡片中心 → 一直往下滑到最底部 →「插件」→ 选「校园助理」**；原生 Android、小米、华为等系统也可以直接用工作台卡片上的「添加到桌面」，或长按桌面 →「小组件」。OPPO / 一加 / realme 的桌面长按菜单里**没有「小组件」这一项**，请按前面那条走。启动页自 1.2.2 起改用应用图标，不再显示旧的自绘标志。

**Q14：登录页没有「手机号登录」，或者收不到验证码？**
手机号登录走的是学校统一身份认证的手机号通道，只有学校开通了才会显示这个标签页；没有该标签页说明学校未开放。收不到短信时先确认手机号已在学校统一身份认证里绑定、且没有被短信平台限流，稍等重试；仍然不行就用「密码登录」或「Cookie 登录」。

**Q15：活动附件打不开，提示「旧版 .doc 是二进制格式」？**
内置查看器支持 `.docx` / `.xlsx` / `.csv` / `.pdf` / 图片 / 文本。旧版 `.doc` / `.xls` 是二进制格式，需要完整的解析器，代价过大，因此会提示用 WPS / Office 打开 —— 用附件行上的「用其它应用打开」即可。下载上限 25MB，超限的附件事先会提示。

**Q16：一卡通（校园卡）里看不到电费？**
先确认已登录（页面顶部会提示去登录）。然后在「电费项目」里选对那一套（如「1-6单元电控缴费」与「7-11单元及东区电控缴费」），选好即出数。学校的电费接口只按费项返回，**没有"选校区/楼栋/房间"这一步**；房间号在学校的后台与你的账号绑定，界面会显示接口返回的房间名。若接口返回但识别不出数值，界面显示「暂时无法获取」而不是 0。需要换房间或缴费，点「在官方页面查看/缴纳电费」。

**Q17：一卡通的消费明细为什么只有汇总？**
逐笔流水在校方的账单系统里（另一套页面，需要单独保持登录态），应用无法在本地复刻。所以「一卡通」页只给按月的**支出 / 收入汇总**，需要逐笔明细时点「查看逐笔明细（官方页面）」，由校方页面渲染 —— 应用不复制你的账单数据。

**Q18：一卡通登录后令牌会过期吗？**
一卡通的登录令牌有有效期，过期后页面会提示需要重新登录（点一下重新走一次统一身份认证即可）。应用**刻意不做静默续期**：那需要保存你的一卡通密码。代价是令牌过期后，电费低余额提醒会暂停，直到你再次打开一卡通页面完成登录。

**Q19：会提示「加载失败」，是不是没给定位权限？**
不是，**和定位权限没有任何关系**。一卡通的数据都是向学校服务器查来的（不是从你手机定位推算的），这条路只需要联网，应用也**没有申请任何定位权限** —— 你去系统设置里找不到可以开的开关，开别的权限也不会有帮助。

现在这条提示会带上真实原因，对照处理即可：

- 提示里带**「登录已失效」** —— 会话过期了，点页面上的「去登录」重走一次统一身份认证。
- 提示里带**「无法连接一卡通系统」** —— 本机到学校的网络不通（校园网/VPN/WiFi），换网络或稍后重试。
- 提示里带**HTTP 500 / 502** 一类 —— 是学校服务端的问题，只能等它恢复，重试再多次也一样。
- 提示里带**「返回了无法识别的数据」** —— 学校系统改版了，属于需要更新应用的情况，可以反馈。

若是纯「加载失败」而没有任何原因，多半是令牌已过期但界面还停在登录态，取消弹窗后回到页面顶部点「去登录」即可。

**Q20：挂失校园卡要输密码，会不会被存下来？**
不会。挂失 / 解挂时的密码只在**当次弹窗内**存在，弹窗一关就随之丢弃，不写任何存储 —— 这和应用登录时处理一卡通密码的方式完全一致（代码里根本不存在保存一卡通密码的接口，由单测护栏守住）。之所以要密码，是因为学校一卡通系统本身的挂失接口可能要求校验；学校没配这项要求时，服务端会忽略该字段，不影响结果。

**Q21：我点了挂失但卡还在手上，能撤销吗？**
能。挂失后卡片状态会显示为「已挂失」，底部按钮会变成「解挂校园卡」，走一遍同样的两段确认即可恢复。注意**解挂同样需要密码**，且挂失期间卡是冻结的、不能消费 —— 所以卡还在手上时请先别挂失。

**Q22：为什么挂失要输一整句话，而不是点个「确定」？**
因为挂失**不可逆**（冻结后没有自助撤销，补卡要去卡务中心），一次手滑的代价是当天刷不了饭卡。要求你逐字打出一整句「我确认挂失这张校园卡」，能同时挡住三件事：误触连点、自动化脚本，以及"没看清就直接确定"。打这句话的过程本身就必须让你读到操作与后果。

**Q23：二课成绩单里的「对不上」是什么意思？**
意思是应用把三层数字对了一遍没对平：**总学时**、两个维度的**小计**、以及**逐条明细**相加的结果。三者应当一致；不一致就说明服务端返回的数据本身有缺口或不一致（应用没有做任何四舍五入或删减）。对账标记会同时告诉你差额出在**哪个维度**（分类还是学期），方便判断是接口少返了记录还是某一维归类有误。属于可以反馈的情况，请连同对账标记一起截图。

**Q24：二课的「5.16」和「25.80」为什么对不上？**
因为它们是**两个量纲**：5.16 是**折算积分**（成绩单顶部那个「总积分」），25.80 是**学时**。学时按各模块权重折算后才变成积分，两者本就不该相等。所以对账行里用的全是学时，折算积分单独占一行并标注清楚 —— 把两者并排比较会觉得"差了 20 分"，其实是口径不同。

**Q25：空闲教室为什么一次只显示 100 间？**
一次查询取 100 条，标题右侧的「共 N 间」是**命中总数**，结果末尾的「加载更多」继续取下一页。如果学校服务器对分页参数不响应，「加载更多」会提示「教务系统没有返回更多结果」，不会让你以为"总共就这么几间"。遇到这种情况，缩小范围（选具体楼号或场地类别、少选几个周次）比翻页更有效。

**Q26：空闲教室能直接借用吗？**
不能，本应用只做**查询**。网页上的「场地借用申请」是向学校提交的写请求，本应用不代提交。要借用场地请到教务系统网页操作。

**Q27：课程提醒到点没响，怎么查？**
按顺序看「课表设置 → 课程提醒 → 权限自检」的四项：**通知权限、精确闹钟权限、后台运行和自启、忽略电池优化**，任意一项没开提醒都可能不准时甚至完全不响。四项都开了还不响，再依次确认：课程详情页里的开关是打开的、总开关没被关掉、该课有有效周次、第一周开始日期与节次时间填过（详情页会直接写出缺哪一项）。

**Q28：上课自动模式会不会把手机一直留在勿扰？**
不会。应用只在自己改过系统设置时才去还原，而且**每轮对账（打开应用、开机、改时间、下课闹钟）都会重新核对一次**：不在课时内、或你把档位改成「关闭」、或总开关关掉，都会立刻还原成你原来的档位。没有「勿扰模式权限」时它根本不会去改系统设置 —— 因为改了就可能还原不了。另外它是**设备级**设置，不随账号变化。

**Q29：提前提醒时间改了，为什么之前的课程还是老时间响？**
不会。提前时间是全局设置，改完会把所有已设的提醒一起按新时间重排，课程详情页上显示的也是新时间。如果详情页显示的还是旧值，说明那一页没刷新 —— 退出重进即可。

---

## 15. 注意事项与免责声明

- 应用不会绕过学校设置的任何限制：验证码、结果待确认、登录失效等需要人工处理的环节都会停下来等你操作；名额、成绩认定、缴费与报名规则最终由学校决定。
- 选课功能已从本应用移除，需要选课请使用学校教务网页（见第 3 章的版本说明）。
- 项目基于 GPL-3.0 开源，仅供学习交流，使用产生的任何后果自行负责；不接受私自打包分发。
- 退课、退报、删除账号等操作不可恢复，请确认后再执行。

---

*本手册基于 v1.2.6 源码整理；若界面与描述不一致，以实际版本为准。*
